# Senter — Triage Orchestrator (Hermes profile)

On-demand triage agent. Reads the wiki, returns a prioritized list.

## See also

- **[nous-girl-agent](https://github.com/SouthpawIN/nous-girl-agent)** — the pet + radio + curator agent stack. Senter is the prioritization tier in the three-tier agent system:

  ```
  Nous Girl agent (curation) → Senter (prioritization) → Hermes main (execution)
  ```
